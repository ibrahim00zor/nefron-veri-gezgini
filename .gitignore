# Yedek arşivleri yerel dursun (büyük, repo'ya gerek yok)
yedekler/

# Ham txt çıktıları yerel dursun — uygulama Parquet'i okuyor, ham gerekmez
# (Reproducibility istenirse README'de yeniden üretme talimatı var)
veri/ham/
veri/ham_scenarios/

# Büyük veri arşivi (~90MB) — repo'ya girmemeli; yanlış `git add .`'i önler
# GitHub 100MB'da dosyayı reddeder, 50MB'da uyarır
veri.zip
*.zip

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# macOS / editör
.DS_Store
.vscode/
.idea/

# Streamlit
.streamlit/secrets.toml

# editör cruft
.Rhistory

# Kişisel günlük — public repo'da paylaşılmaz, sadece lokal düzen için
notlar/gunluk.md

# Ufuk hipotezi — özgün, geliştirilmemiş fikir; öncelik koruması için lokal/gizli
notlar/ufuk_hipotezi.md

# Klinik vizyon — özgün ana strateji; geliştirilmekte, öncelik koruması için lokal/gizli
notlar/klinik_vizyon.md

# Düşünce defteri — kişisel felsefi notlar; lokal/gizli
notlar/dusunce_defteri.md

# Okuma listesi — kişisel çalışma planı; lokal/gizli
notlar/okuma_listesi.md

# Segment atlası — TASLAK bilimsel belge; literatür iddiaları henüz çift-kontrol
# edilmedi (yalnız PT, S3 doğrulandı). Beraber doğrulanınca public yapılabilir.
notlar/segment_atlasi.md

# D3 onizleme render (gecici, veri enjekte edilmis; repo disi)
kod/d3_components/_onizleme_render.html
